-- Create database for Django project
-- Run this in MySQL command line or MySQL Workbench

CREATE DATABASE IF NOT EXISTS magazine_db 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Grant privileges (adjust username/password as needed)
-- GRANT ALL PRIVILEGES ON magazine_db.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;

